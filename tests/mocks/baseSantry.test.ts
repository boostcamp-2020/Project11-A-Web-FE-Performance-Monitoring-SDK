import { VanillaSantry } from '../../src/VanillaSantry';
test('Error Context parse', () => {
  const mokeSantry = new VanillaSantry('token');
  // console.log(mokeSantry.getErrorContext(new Error('test')));
});
